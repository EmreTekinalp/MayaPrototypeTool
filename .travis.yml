language: python

# python versions to consider
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"

# install dependencies
install:
  pip install -r requirements.txt

# run scripts
script:
  - nosetests --with-coverage --cover-erase --cover-html --cover-package=MayaPrototypeTool
